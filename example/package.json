{
  "name": "example",
  "private": true,
  "description": "Example project using mapo",
  "scripts": {
    "dev": "env NODE_OPTIONS=--openssl-legacy-provider nuxt",
    "build": "env NODE_OPTIONS=--openssl-legacy-provider nuxt build",
    "start": "env NODE_OPTIONS=--openssl-legacy-provider nuxt start",
    "generate": "env NODE_OPTIONS=--openssl-legacy-provider nuxt generate",
    "test": "echo \"No test defined\""
  },
  "dependencies": {
    "@mapomodule/mapo-integrations-camomilla": "1.0.0-alpha.11",
    "mapomodule": "latest",
    "nuxt": "^2.17.3"
  }
}

